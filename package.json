{
  "name": "speedruns",
  "version": "0.20.20",
  "author": "Jeremy Banks <_@jeremy.ca>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@apollo/react-hooks": "^3.1.3",
    "@types/jest": "^24.0.0",
    "@types/node": "^12.0.0",
    "@types/react": "^16.9.0",
    "@types/react-dom": "^16.9.0",
    "apollo": "^2.21.2",
    "apollo-cache-inmemory": "^1.6.5",
    "apollo-client": "^2.6.8",
    "apollo-link-error": "^1.1.12",
    "apollo-link-http": "^1.5.16",
    "apollo-link-retry": "^2.2.15",
    "apollo-link-schema": "^1.2.4",
    "graphql": "^14.5.8",
    "graphql-cli": "^3.0.14",
    "graphql-tag": "^2.10.1",
    "prettier": "^1.19.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.3.0",
    "typescript": "^3.7.4"
  },
  "scripts": {
    "generate": "bash scripts/generate.bash",
    "format": "bash scripts/format.bash",
    "start": "bash scripts/start.bash",
    "precommit": "yarn generate && yarn format && echo 'grep -q precommit package.json && (yarn precommit && git add .) || echo' > .git/hooks/yarn-precommit && chmod +x .git/hooks/yarn-precommit",
    "build": "react-scripts build",
    "test": "react-scripts test"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      "firefox esr",
      "last 1 firefox version",
      "last 1 edge version",
      "last 1 ios version",
      "last 1 android version",
      "last 1 chrome version"
    ],
    "development": [
      "last 1 firefox version",
      "last 1 chrome version"
    ]
  }
}
